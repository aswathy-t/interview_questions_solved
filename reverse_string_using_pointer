

#include<stdio.h>
#include<stdlib.h>
int main()
{
    char* ptr=(char*)malloc(4*sizeof(char));
     for(int i=4;i>0;i--)
    {
        scanf("%c",ptr+i);
    }
    
    for(int i=4;i>0;i--)
    {
        printf("%c",*(ptr+i));
    }

    
    
    return 0;
}
